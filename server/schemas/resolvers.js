const { tagSchema} = require("../models");
const resolvers = {
    Query: {
      
    }
  };

  module.exports = resolvers;

  /*
  tags: async () => {
        return tagSchema.find();
      }
      */